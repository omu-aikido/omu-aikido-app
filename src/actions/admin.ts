import { defineAction } from "astro:actions";

export const admin = {
    user: {
        // add: defineAction({}),
        // update: defineAction({}),
        // delete: defineAction({}),
        // list: defineAction({}),
    },
    record: {
        // add: defineAction({}),
        // update: defineAction({}),
        // delete: defineAction({}),
        // list: defineAction({}),
        // recent: defineAction({}),
    },
};
