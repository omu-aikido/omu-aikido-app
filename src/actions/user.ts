import { defineAction } from "astro:actions";

export const user = {
    // add: defineAction({
    //     handler: (input, context) => {
    //         return { success: true };
    //     },
    // }),
    // update: defineAction({}),
    // delete: defineAction({}),
};
