---
import { style } from "@/src/styles/component"
---

<div class="animate-pulse">
  <form class={style.form.container({})}>
    <label class="text-sm font-medium">
      <div class="h-4 bg-gray-300 rounded w-16 mb-1"></div>
    </label>
    <div class={`${style.form.input({})} bg-gray-200 h-10`}></div>

    <label class="text-sm font-medium">
      <div class="h-4 bg-gray-300 rounded w-20 mb-1"></div>
    </label>
    <div class={`${style.form.input({})} bg-gray-200 h-10`}></div>

    <div class={`${style.form.button()} bg-gray-300 h-10 flex items-center justify-center`}>
      <div class="h-4 bg-gray-400 rounded w-16"></div>
    </div>
  </form>
</div>
