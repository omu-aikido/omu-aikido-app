---
import { ReactHeader } from "@/./react-router-app/components/component/Header"
import { AccountUi } from "@/rr7/components/component/AccountUi"
import { Sidebar } from "@/rr7/components/ui/Sidebar"
import type { PagePath } from "@/src/type"

interface Props {
  title: string
  homelink: string
  textLinks: PagePath[]
}
const { title, homelink, textLinks } = Astro.props
---

<ReactHeader title={title} homelink={homelink} textLinks={textLinks}>
  <Sidebar position="right" icon={"  ≡  "} client:load>
    <AccountUi apps={textLinks} />
  </Sidebar>
</ReactHeader>
